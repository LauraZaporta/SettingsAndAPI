CREATE TABLE bars (
    latitude REAL NOT NULL,
    longitude REAL NOT NULL,
    points INTEGER CHECK (points>=0 AND points <= 10),
    title TEXT,
    description TEXT,
    image TEXT
);

selectAll:
SELECT * FROM bars;